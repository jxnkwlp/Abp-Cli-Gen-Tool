using System;
using System.Threading.Tasks;
using Microsoft.AspNetCore.Mvc;
using Volo.Abp;
using Volo.Abp.Application.Dtos;

namespace {{Namespace}}
{
    [RemoteService]
    [Route("api/{{Name}}")]
    public class {{Name}}Controller : {{ProjectName}}Controller, I{{Name}}AppService
    {
        private readonly I{{Name}}AppService _service;

        public {{Name}}Controller(I{{Name}}AppService service)
        {
            _service = service;
        }

    }
}
